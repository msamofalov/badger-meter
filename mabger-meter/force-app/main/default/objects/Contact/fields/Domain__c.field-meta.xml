<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Domain__c</fullName>
    <description>this will capture the domain portion of a users email address.  This is used to match new contacts created via community self registration to their correct accounts.</description>
    <externalId>false</externalId>
    <formula>RIGHT(Email, len(Email) - FIND(&quot;@&quot;, Email, 1))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>this will capture the domain portion of a users email address.  This is used to match new contacts created via community self registration to their correct accounts.</inlineHelpText>
    <label>Domain</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
